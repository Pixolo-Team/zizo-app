"use client";

// Interface Props
interface ImageCardProps {
  text: string;
}

/** ImageCard Component */
export default function ImageCard({ text = "" }: ImageCardProps) {
  // Define Navigation

  // Define Context

  // Define States

  // Define Refs

  // Helper Functions

  // Use Effects
  return <div>{text}</div>;
}
